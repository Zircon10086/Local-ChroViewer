diff --git a/dist/esm/Match.js b/dist/esm/Match.js
index 8da8d0f108b2dcd57fc6c9a853317d164e31776b..db5ba52f642865f08b3b867c674b4f03738b3412 100644
--- a/dist/esm/Match.js
+++ b/dist/esm/Match.js
@@ -125,7 +125,7 @@ function OnRendered() {
 var MatchInner = React$1.memo(function MatchInnerImpl({ matchId }) {
 	const router = useRouter();
 	const getMatchPromise = (match, key) => {
-		return router.getMatch(match.id)?._nonReactive[key] ?? match._nonReactive[key];
+		return router.getMatch(match.id)?._nonReactive[key] ?? match._nonReactive[key] ?? new Promise(() => {});
 	};
 	if (isServer ?? router.isServer) {
 		const match = router.stores.matchStores.get(matchId)?.get();
@@ -240,7 +240,7 @@ var MatchInner = React$1.memo(function MatchInnerImpl({ matchId }) {
 			reset: void 0,
 			info: { componentStack: "" }
 		});
-		throw match.error;
+		throw match.error ?? new Error(`match for route "${routeId}" failed without an error value`);
 	}
 	return out;
 });
